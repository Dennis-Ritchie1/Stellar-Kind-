# Infrastructure

This folder contains deployment and infrastructure configuration for Stellar-Kind.

## Contents

- `docker/` — Dockerfiles and docker-compose examples
- `kubernetes/` — Kubernetes deployment manifests and services

## Recommended next steps

- Add `Dockerfile` for `frontend`, `backend`, and `contract` build pipelines
- Add `docker-compose.yml` for local development
- Add Kubernetes manifests for staging and production
